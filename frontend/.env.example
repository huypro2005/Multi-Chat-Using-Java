# Local dev (npm run dev) — gọi thẳng backend port 8080
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_URL=http://localhost:8080/ws

# Docker build KHÔNG dùng file này (.env bị .dockerignore).
# Dockerfile set VITE_API_BASE_URL= (rỗng) và VITE_WS_URL=/ws tự động.
